#!/usr/bin/env ruby
require_relative '../lib/turn'

puts "Where do you want to move?"
index = gets.strip
if valid_move? == true
  move (board, index)
  display_board(board)
else puts "Invalid move. Please select another move."
end
